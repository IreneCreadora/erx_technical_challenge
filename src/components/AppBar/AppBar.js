import { Navigation } from '../Navigation/Navigation';

export const AppBar = () => {
  return (
    <header className="relative flex justify-center items-center px-2 py-2 bg-teal-700 mb-3 shadow-md">
      <Navigation />
    </header>
  );
};
